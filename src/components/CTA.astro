---
const { isLink, href } = Astro.props;
---

{
  isLink ? (
    <a href={href} class="cta">
      <slot />
    </a>
  ) : (
    <button class="cta">
      <slot />
    </button>
  )
}

<style>
  .cta {
    display: inline-block;
    outline: 2px solid var(--accent, black);
    padding: 0.5rem 1rem;
    margin-block: 0.5rem;
    transition: background-color 200ms ease;
  }

  .cta:hover {
    outline: none;
    background: var(--text);
    color: var(--background);
  }

  a.cta {
    color: inherit;
    text-decoration: none;
  }
</style>
